# Set this at frontend build time when the API is hosted on a separate origin.
# Leave unset to use same-origin /api in production or localhost:8000 in development.
# REACT_APP_API_BASE_URL=https://api.example.com/api
